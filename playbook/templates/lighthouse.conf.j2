server {
    listen 80 default_server;
    listen [::]:80 default_server;

    server_name _;

    root {{ lighthouse_dir }};
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}